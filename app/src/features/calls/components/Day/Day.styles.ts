import { styled } from '../../../../global.styles';

export const Day = styled('div', {
  display: 'block',
});

export const DaySection = styled('section', {
  display: 'grid',
  gridTemplateColumns: '1fr 1fr',
  gridGap: '1rem',
});

export const DaySectionColumn = styled('div', {
});

export const DaySectionTitle = styled('h3', {
});

export const DaySectionSubtitle = styled('h4', {
});
